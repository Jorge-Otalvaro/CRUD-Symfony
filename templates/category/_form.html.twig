{{ form_start(form) }}

    {{ form_widget(form) }}

    <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>

    <a href="{{ path('app_category_index') }}" class="btn btn-info">Volver al indice de categorías</a>

{{ form_end(form) }}
