{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %}

{% block body %}

    <h1>{{ product.name }}</h1>

    <table class="table table-striped">
        <tbody>

            <tr>
                <th>Código</th>
                <td>{{ product.code }}</td>
            </tr>

            <tr>
                <th>Descripción</th>
                <td>{{ product.description }}</td>
            </tr>

            <tr>
                <th>Marca</th>
                <td>{{ product.brand }}</td>
            </tr>

            <tr>
                <th>Precio</th>
                <td>{{ product.price }}</td>
            </tr>

            <tr>
                <th>Fecha de creación</th>
                <td>{{ product.createdAt ? product.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

            <tr>
                <th>Fecha de actualización</th>
                <td>{{ product.updatedAt ? product.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="card-body" style="text-align: right;">   
        <a href="{{ path('app_product_index') }}" class="btn btn-info btn-sm ml-auto">Volver al indice de productos</a>

        <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="btn btn-success btn-sm ml-auto">Editar el producto</a>        
    </div>

    {{ include('product/_delete_form.html.twig') }}

{% endblock %}
